#include<iostream>
using namespace std;

bool subsets(int *in,int i,int n){
    
    if(i==n){
        if(sum==0)
            return true;
    }
    int sum=0;
    //recursive case
    //include ith element
    sum+=in[i];
}